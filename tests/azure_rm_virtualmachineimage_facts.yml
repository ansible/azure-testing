- name: Test azure_rm_virtualmachineimage_facts
  hosts: all
  connection: local
  vars:
      location: eastus
      playbook_debug: yes 
  tasks:

      - name: List all versions
        azure_rm_virtualmachineimage_facts:
            location: "{{ location }}"
            publisher: OpenLogic
            offer: CentOS
            sku: 7.1
        register: output
      
      - debug: var=output
        when: playbook_debug
  
